import { styled } from "@mui/material";

export const MainContainer = styled("main")(() => ({
  flexGrow: 1,
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  height: "100%",
  overflow: "auto",
  paddingBottom: "1rem",
}));
